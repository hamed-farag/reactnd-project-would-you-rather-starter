import consts from "../consts/user";

const initialState = {
  loggedInUser: {},
  users: [],
};

export default function (state = initialState, action) {
  switch (action.type) {
    default:
      return state;
  }
}
